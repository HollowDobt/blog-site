---
import Comments from "./Section.svelte";

interface Props {
	locale: string;
	link: string;
	section: string;
	item: string;
	compact?: boolean;
}

const { locale, link, section, item, compact } = Astro.props;
---

<Comments client:visible {locale} {link} {section} {item} {compact} />
